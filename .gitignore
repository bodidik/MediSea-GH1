# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# package-lock.json BİLEREK yok sayılmıyor: CI her iki işte de `npm ci`
# çalıştırıyor ve `npm ci` lockfile olmadan çalışmaz. Yok sayıldığı sürece
# setup-node "Some specified paths were not resolved" ile kırılıyordu, temiz
# klon ise hiç kurulum yapamıyordu. Sürüm sabitlemesi için takip edilir.
pnpm-lock.yaml

# Logs
logs
*.log
*.log.*
lerna-debug.log

# Build output
.next/
out/
dist/
build/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Mac / Windows junk
.DS_Store
Thumbs.db

# Coverage
coverage/
# --- mediSea web ignores ---
web/.next/
web/dist/
web/coverage/
web/tsconfig.tsbuildinfo
web/*.tsbuildinfo
web/*.bak
web/*ts-error*.txt
web/node_modules.zip

web/.env.local

server/.env
server/.env
web/.env.local
web/.vercel/

# Catch-all env variants
.env.*
!.env.example
!.env.*.example

# Runtime data
data/
# ...ama bu bir kaynak dizini, çalışma zamanı verisi değil (build ona bağlı)
!web/app/tools/data/
tmp/
temp/
uploads/

# Catch-all env variants
.env.*
!.env.example
!.env.*.example

